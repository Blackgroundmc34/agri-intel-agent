# Move from the frontend folder up one level, and then into the backend folder
cd ../backend

# Activate the Python virtual environment (very important!)
source venv/Scripts/activate

# Now, run the uvicorn server from the correct directory
uvicorn main:app --reload /  uvicorn main:app --reload --host 0.0.0.0 --port 8000
